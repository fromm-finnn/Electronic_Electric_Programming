#include <stdio.h>

main()
{
	int n, matrix[100][100] = { 0 }, i, j;

	printf("Input the n of n*n matrix : ");
	scanf_s("%d", &n);

	for (i = 0; i < n; i++)
	{
		for (j = 0; j < n; j++)
		{
			printf("Input the number of matrix[%d][%d]\n", i + 1, j + 1);
			scanf_s

		}
	}
}